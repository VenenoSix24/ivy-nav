# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- **图标在图标遮罩里怎么摆，可以选了**（设置 → 条目图标，默认「原样」＝改之前的行为）：
  - **原样**：整张图按比例放进去，四周留一圈呼吸位（默认，也是唯一留白的）
  - **自动裁边**：在浏览器里把图标画进一张 96×96 的画布，数出不透明像素的范围，
    裁掉四周的透明边、让图案顶到板边（放大倍数额外封顶 2 倍，图案只占三成以下时按「量不准」放过）
  - **裁剪铺满**：放大到铺满图标遮罩，超出裁掉
  - **拉伸铺满**：拉到和图标遮罩一样大，不是正方形会被压扁

  后三档都是「填满」：图形直接顶到板边（早先这一档也留七成，于是自带白底的应用类图标
  选了「铺满」仍然够不着板边 —— 那种图内容本来就占满画布，没有可裁的透明边）。
  单个条目可以在图标选择器里覆盖（只在图片类图标上出现）；条目没设过就跟随设置页里的默认，
  设置页改一次，那些没动过的条目一起变。条目自己那份跟着备份走（`items.icon_fit`）

- **分类也在首页改**：每个分区标题右边多一个「⋯」—— 名称与描述、这一组用哪套布局、
  上移 / 下移 / 移到最前、从首页隐藏、整类隐藏、删除分类，全都在看着这一组内容的地方改，
  改完立刻生效。设置页里原来那两节（首页分类、首页布局）随之删掉，只留一句指路；
  首页的编辑工具条上，「分类设置」这个跳去设置页的链接也换成了「整理分类」
- **整理分类**：新的排序入口。分类不再是竖排长名单 —— 一屏摊成**两列的紧凑格子**，
  拖到哪儿都只是一小段距离，顺带看得见谁被隐藏了；每行右边是铅笔（改名字与描述），
  底部能直接新建分类。分区「⋯」里的上移 / 下移按**首页上真正看得见的那几个分区**换位，
  中间夹着的空分类不会让点击看起来没反应
- **导入浏览器书签**（设置 → 浏览器书签）：Chrome / Edge / Firefox / Safari 导出的
  `bookmarks.html` 都能读。一级目录当分类，二级及更深的目录当标签（`A/B/C` → 分类 A、
  标签 B 与 C），浏览器自带的那层壳（书签栏 / 其他书签 / Bookmarks bar…）忽略掉直接下钻，
  顶层散着的书签落进未分类。标题里的 `&amp;` 之类的实体、笔记式的 `<DD>` 说明都会读。
  选好文件先出**一张预览**（分类与标签清单、哪些新建哪些复用、哪些行读不了、前几条的样例），
  确认之后才写库，只新增与更新、不删除现有内容
- **可以只导其中几个目录**：预览里每个顶层目录一行、带勾选框（默认全选），想分几批导入就取消掉
  这次不要的那些目录、下次再挑剩下的。预览里的数字跟着勾选走，不必提交一次才知道会导入多少
- 重复网址按**「分类 + 网址」**判重：同一个网址出现在两个分类里**各建一条**（合并成一条会让
  另一个分类少一条），预览里单独提示有多少个这样的网址、多出多少条；只有同一个分类里出现两次
  才只留第一次。库里已经有且就在那个分类里时，默认跳过，「覆盖已有条目」会用书签里的标题与描述
  更新它，标签是并进去而不是换掉（用户自己打的标签不会丢）
- 书签里超过本站上限的部分（标题 80 字、描述 300 字、标签 12 个）按上限截断，预览里写明
  截了多少条；`http`/`https` 之外的网址（`javascript:` 之类）单独列出来，不会写进库
- 图标不在导入时抓取：几百条会把外网拉爆，交给首页按需缓存
- 图标**图标遮罩可以按条目关掉**：卡片图标底下那层描边与玻璃底，遇上自带圆角外形的应用类图标
  就成了大圆套小圆。选择器顶部多一个「图标遮罩」开关，关掉后格子尺寸不变、只是不再画那层底；
  图形大小改成按盒子算（容器查询单位），关掉图标遮罩后自己涨到接近满格 —— 不然只剩中间那一小张，
  看着比原来小一圈
- 单色图标可以**跟随主题**（只管深色那一套）：浅色下照原样显示品牌色/原色，深色下把那张
  SVG 当成 alpha 蒙版、颜色交给 `currentColor`，于是黑图在深色主题里也不会消失
  （GitHub 的品牌色 #181717 就是这种）。两个主题各渲染一份、用 CSS 的 `dark:` 切换，
  不闪、不用 JS，也不必为两套主题各存一个文件。只对 SVG 生效：位图的 alpha 是整个方块，
  蒙出来会是一块实心色
- 输入网址之后的**图标也自己取了**：「自动」那一档在网址合法后 1.5 秒自己去抓一次候选
  （同一个网址只抓一次，且只在这一档上抓），不必先点按钮 —— 按钮留着当「重新获取」
- 图标库的搜索框挪到那排图标库胶囊**上面**：关键词只输一遍，下面换库接着搜同一个词。
  打开面板时还会按标题或域名先填一个最像名字的词（`GitHub · Where software is built` → GitHub；
  中文标题就取域名，`bilibili.com` → bilibili），不满意再改
- 搜索结果**可以接着往下看**：一页 60 个，底部写明「显示 60 / 1234」，还有「显示更多」接着要
- 图标库的关键词跟着**标题**走：标题按网址取回来之后，搜索框里立刻就有词了，不用等保存再重开
  （在框里打过字就以用户的为准）
- 图标库这一档重做，默认挂三套、按优先级排：**Simple Icons**（数据随 npm 包装在本地，三千多个
  品牌标志，自带品牌色与品牌规范链接，搜索与取图都不碰网络）、**Iconify**（官方 search / svg
  接口，二十万+ 图标，结果里标出图标集与它的许可）、**Lucide**（本地登记表，前端直接渲染）。
  一次只搜一页、只取挑中的那一张 —— 全量搬回来是不可能也不必要的事。颜色按各家规矩给：
  Simple Icons 用它的品牌色 `hex`（也可选黑白），单色套件可改色，彩色套件保持原样
- 挑中的那张**下载到本地上传目录**，之后就是一张普通的上传图片：离线能显示，也不再依赖那个
  图标库。文件名前缀写着来路（`simple-icons-github-181717_<hash>.svg`），同一个库、名字、颜色
  算出来总同一个名字，重复挑不会堆文件；这也顺带把许可问题放平了 —— 图标本体落在用户自己的
  数据目录里，我们的仓库不放别人的图标
- **自建图标集**：填一份 JSON 地址（`{name, icons: [{name, url}]}`，也接受数组或 `{名字: 地址}`），
  抓回来只把清单存进 `icon_sets`。设置页有「图标库」一节管增删与重抓，编辑条目的选择器里也能
  用「+ 图标集」直接加；地址在 `raw.githubusercontent.com` 上时可勾「镜像加速」改走 jsDelivr
  （实测本机 raw.githubusercontent 完全连不上、jsDelivr 通）
- 图标选择器的「自动」不再只给一张：点「获取图标」会把每个方案各取一张摆出来（网站声明、
  PWA 清单、/favicon.ico、第三方兜底），取不到的与只回占位图的也照样占一格并写明原因，
  点哪张就用哪张。选中的方案记在条目的 `iconValue` 上，显示的接口按它取图；那个来源哪天
  失效了自动退回链子，不会让图标位空着
- 图标选择器多了「图标库」一档：先选哪一套图标库，再在下面挑具体那一个（Lucide 不再和
  Emoji、上传并列）
- 分类可以**整类隐藏**：设置页「首页分类」每个分类多了一个「整类隐藏」开关，打开后这个分类连同
  里面的条目一起对匿名访客收起（登录后照旧可见）。过滤在服务端做，匿名响应里没有这些行
- 取站点图标多两级来源：网页里声明的 PWA manifest 图标，以及直连失败时去第三方图标服务
  兜底（favicon.im → icon.horse，可用 `FAVICON_FALLBACK_SOURCES=false` 关掉）。实测 23 个
  常用站点从 18 个拿到图标提升到 22 个（剩下那个 example.com 是真的没有图标）
- 只填了网址也能保存条目：标题会照网页的 `og:site_name` / `<title>` 自动填上（`/api/site-meta`，
  与取图标共用同一套出口检查），读不到就提示手填；保存时标题若仍为空，用域名兜底
- 图标选择器里加了明确的「获取图标」按钮：点一下才去抓，抓到了显示图标、抓不到直接说抓不到
  并给出替代做法（改用 Emoji / Lucide / 上传，或留空显示首字母）
- 浮层有了自己的玻璃材质（`surface-float`）：弹窗、卡片菜单、下拉都从实色换成磨砂，
  比卡面更实一点，压在上面的正文留得住对比度
- 编辑模式的动作按钮（「添加」「新建项目」）与 `outline` 变体改走玻璃：它们浮在首页内容之上，
  不透明的底色看着像贴上去的一块
- 分类描述在手机上也显示：窄屏与标题并排放不下，描述自己占一行；宽屏仍是标题右边那行小字
- 登录页加上品牌标记（48px 的叶子），与顶栏、页脚用同一份图
- 首页布局改为按分类各自设置：一个分类常常是另一种用法（项目摊开看描述、工具紧凑当入口、
  资料多则列表扫）。取值存在分类上（空值即默认卡片），跟着备份走，设置页里每行一个分类三选一
- 列表布局重做：域名挪到标题右侧、标签挪到描述右侧，两行铺满整行宽度，行高从 54px 提到
  桌面 68px / 手机 84px。窄屏不显示域名，描述与标签各占一行（并排会把描述挤成「本…」这种残句）
- 编辑模式的工具条改为悬浮跟随，与顶栏留 19px 距离；点「完成」给一次「改动已保存」的提示
- 首页条目可在设置里切换三种布局：卡片（默认）、列表、紧凑。三种形状共用一份网格参数，
  公开视图与编辑视图不再各写一套列数；列表在窄屏只留标题、域名与「打开」，比两列卡片更好扫视，
  紧凑则放到手机三列、桌面六列
- 六套可切换的配色主题（一叶青、天青、紫罗兰、陶土、玫红、石墨），与浅色/深色正交：只换强调色与
  背景光晕，中性色与卡面材质共用一套。取值全部按 AA 校对，并有一条测试盯着 CSS 与配色清单不走散
- 站点有了正式的品牌标记：一片叶子。标签页图标、Apple 图标与分享卡片（1200×630）都由
  `pnpm brand:assets` 从 `scripts/brand/source/yiye.png` 生成，换 logo 只需换源图重跑
- 标签页图标用带深青图标遮罩的方块版本 —— 透明叶子放在标签栏上，亮部对浅色底只有 1.6:1 对比，
  几乎看不见
- `pnpm admin:create`：交互式建号，支持 `ADMIN_PASSWORD` 非交互传入与 `--reset` 重置密码
- `pnpm test` 之外的部署路径实测：整项目部署与 standalone 产物两种方式都写进 README

### Changed

- **浅色下，显示图标的那个方块浮起来一点**（深色下不加），按两种情形分：
  **有图标遮罩**就加在图标遮罩上（分层柔和阴影：模糊 2 → 4 → 8、透明度 7% → 5% → 3%，近处锐、远处散）；
  **没有图标遮罩**时方框没有面，加在它身上会变成一块悬在图标背后的灰方块（emoji 看着就像又垫了
  一层板），所以那种情况加在图形自己身上（`drop-shadow`，跟着图案轮廓走）。
  深色下卡面本来就暗，再加影子只会脏，两种情况都关掉
- **选了「铺满」那几档，图形真的顶到板边了**：整块板只在「原样」档画那圈描边 ——
  描边画在盒子内侧，留着它图形就永远差一圈；裁边的门槛从「占了九成四就不动」放松到九成八；
  量透明边距的画布从 48 放大到 96（48 的时候一个像素就是整幅的 2%，量化误差正好会留下
  「一点点边距」），裁完再多铺 3%（过扫），多出来的由外层裁掉 —— 屏幕上的「正好填满」
  总还是差一条细边，多铺一点才是肉眼上的踩满
- **没有图标遮罩时也有影子了**：影子原先挂在图片那张 `<img>` 上，于是 emoji 与首字母托底
  （以及没取到图标的情况）都看不到它。改成挂在**图形容器**上：容器里渲染出什么，影子就跟着什么
- **Emoji 一律不套图标遮罩**（选择器里那个「图标遮罩」开关对它不再出现）：emoji 自己就是一块彩色图案，
  再垫一层反而显得歪，而各家字体把图案摆在字框里的位置本来就不一致。摆法也回到朴素的
  `place-items-center` + 行内盒居中 —— 不逐个去量墨迹了，整齐够用就行
- **图标方框的圆角按 Apple 应用图标的比例给**（26%，随格子大小走，原来固定 8px）：
  固定值在大格子上偏方、在小格子上偏圆，比例才是三种布局看起来一致的原因。取 26% 而不是
  Apple 那个 22%：纯 CSS 的圆角比 Apple 的连续圆角（squircle）看着更「方」，要更大一点才是那个观感。
  图标遮罩关掉时也保留（那时方框里直接是一张图）
- **Emoji 字号收到图片类的八成**：它的墨迹本来就比字框大一圈，照足给会显得傻大
- **图标遮罩改回原来的玻璃底 + 描边**（上一轮换的那套中性灰在浅色下确实不好看）。顺带弄清
  「图标看着偏心」的真正原因：标题短又没有描述时，图标比标题长一截，于是左基线看着就歪了 ——
  与图标遮罩用什么颜色无关，所以图标遮罩换回来、图标大小也不动
- 图标选择器「网站图标」那一页不再是「看着像按钮、按下去只是选中态」的写法（那种写法第一眼
  都会以为按坏了）；网址一换，上一次的获取结果也不作数，免得拿旧站点的图标当新站点的
- 标签两行之间的间距 4px → 6px，两行看着不再挤在一起
- 「移动分类」子菜单不再出现滚动条：高度上限给到一屏的六成（分类不多时根本不触发滚动），
  滚动条也藏起来
- 标签分行不再「填满一行再换行」：改为把放得下的标签尽量均分到各行 —— 五个标签是 3 + 2，
  而不是 4 + 1（第一行挤满、第二行空着）。分行由纯函数算（`planTagRows`），宽度取一层
  不可见的量尺，不再有「渲染 → 收敛 → 再渲染」的来回
- 列表里的网址与标题改按**基线**对齐（原来居中对齐，字号小一号的网址看着往上飘）
- 登录页的说明不再提「并看到 Private 内容」
- 标签折叠不再按固定个数：铺满允许的行数（卡片两行、列表一行），放不下的才收成行末一个 `+N`。
  原先固定显示四个 —— 四个短标签只占一行半也会折成 `+1`，第四行后面明明还空着一大片。
  显示几个由量出来的行号决定，卡片宽窄、标签长短都跟着变；容器也把高度锁在允许的行数内，
  `+N` 不会跑到第三行去
- 列表布局：标签从「描述右边」改到**描述下方**，域名、描述、标签三行都从左边排起；行高
  68px → 92px，图标 36px → 44px
- 手机端卡片的「打开」从左下角移到与内容同一条左基线，与上方标签的间距取 1rem —— 和卡片
  下内边距一样宽，按钮上下的留白才一样
- 图标再放大一圈：卡片图形 28px → 36px（手机 30px）、列表 20px → 28px。首字母托底的字号
  取满 `--icon-glyph`（原先只有 0.75 倍，大写字高只有盒子的三成，看着比图片小一圈）；
  Emoji 退回九成，墨迹高度与图片一致
- 列表布局的域名与标签改为**紧随**标题与描述（原先靠右，中间空出一大片，还容易被误读成
  「跟打开按钮有关」）
- 卡片上的图标放大：盒子 44px → 56px，图形与首字母跟着一起放大（图形尺寸做成 `--icon-glyph`，
  只放大盒子而不放大图形反而更空）
- 手机端列表的标签回到描述右侧（不再另起一行），第二个标签在窄屏隐藏 —— 390px 下两个标签
  会把描述挤成「本…」这种没用的残句
- 背景不再只有滚动视差：两团光斑各自慢速漂移并微微胀缩，不滚动时也在呼吸（46s / 58s 两个周期错开），
  并加了一层极细噪点打散渐变色带。只动 `transform`，实测 5 秒内 `LayoutCount` 增量为 0、主线程占用 16ms
- 分类导航补上右缘渐隐，分类多到溢出时能看出右边还有内容；筛选胶囊的状态改用 `aria-pressed`
  （`aria-current` 是给「当前页面」的，用来表示筛选按下对屏幕阅读器是错的）
- 删掉 `/management`：它只是写一个 Cookie 再跳回首页，与设置页的开关重复，而且是在 GET + redirect 里改状态
- 接口回归套件收进仓库：`pnpm build && pnpm e2e`，七个套件各自用独立的临时库与端口
- 顶栏改为滚动感知：停在顶部时完全透明无边线，向下滚动后才浮起毛玻璃与细线
- 分类导航去掉灰色底衬与吸顶，改为搜索栏下方留出间距的一排胶囊
- 卡片标签最多显示 4 个（原先 2 个，右侧明明还有位置就折叠了）
- 弹窗与图标选择器的内部滚动条隐藏
- 外观切换的提示与按钮居中对齐，背景环境光的滚动位移幅度加大到看得出来的程度
- 顶栏只留品牌（带标记）与两个图标按钮，主题切换不再带文字（切档时给一次短暂提示）
- 分类导航从顶栏移到搜索栏下方，改成吸顶条带 + 分段控件，分类多了在控件内横向滚动
- 编辑模式入口移到设置页，用 Cookie 记住状态，跨页面保持
- 标签输入改成胶囊式：回车或逗号成标签、点 × 删除、空输入按退格删最后一个
- 背景环境光随滚动轻微上移（CSS 滚动驱动动画，浏览器不支持时退回静态）
- 新增页脚：品牌、口号、首页/设置入口与年份
- 顶栏分类改为「平铺前三个 + 更多下拉」，选中的分类一定会出现在平铺区；不再显示计数
- 分类导航补回裁剪与滚动，中等宽度下不再盖住右侧的外观与设置按钮
- 分类导航移到常驻顶栏（品牌 / 分类 / 外观 / 设置同排），滚动到任何位置都能切换分类
- 手机端卡片改为两列，并收窄内边距、把标签与 Open 改为上下排布
- 演示种子数据把 7 个分类全部设为首页显示，首次导入即可看到完整排版
- 管理员账号改由服务器端 `pnpm admin:create` 创建，网页端不再提供建号入口

### Removed

- 网页端建号接口 `/api/auth/setup`
- 仓库里的 `AGENTS.md`（`next dev` 每次自动生成，改为忽略）

### Fixed

- **iPhone 悬浮工具栏下面那条白灰带没有了**：iOS 26 的 Safari 不再认 `theme-color`，
  浏览器底色改成它自己在视口边缘「找」—— 扫 `fixed` / `sticky` 元素、读它们的
  `background-color`，读不到就回落到自己那层白。原来那层铺满屏幕的环境光是全透明的，
  正好被读成「没有颜色」。让它带上页面底色、并压到内容之下，那条带就没了
- **桌面端分类条后面的分类点不到**：那一排是横向滚动，但桌面没有触摸滑动，也没有任何提示。
  现在可以**按住整排左右拖**（超过 4px 才算拖动，松手不会顺手切了分类），被挡住的那一端
  会出现圆形箭头，鼠标扫到这一排时才浮出来；滚到中间时两头都渐隐
- **编辑条目时不再每次重开面板都重新抓一遍图标**：自动获取现在只在**打开面板之后新输入的网址**
  上触发 —— 打开面板时就有的那个网址不抓（想看别的候选点「获取图标」即可）
- 在图标库里挑好图标后重开编辑页，页签总回到「自动」：页签是按条目当前的图标推出来的
  （图标库挑来的那张、Lucide、Emoji 各回各档），图标库那一档还会选中当初用的那个库
- 图标库不再默认摊开一批图标：没有关键词就不发请求，搜什么看什么。自建图标集的入口仍是
  一个「+ 图标集」胶囊，点开只有两行
- 在图标选择器里挑好一张、保存后卡片显示的还是旧的那张：图标响应带 7 天缓存，而地址一直是
  `?item=<id>`，浏览器压根没再去问。地址带上所选来源当变更标记，换了来源就是一条新地址
- 登录页也按 `lvh` 排版：它还是 `dvh`，在 iPhone 上同样比屏幕矮一截
- iOS 上悬浮工具栏下方那条素色底，两次都没治到根上：`viewport-fit=cover` 只让布局视口铺满，
  页面却还是按 `dvh` 排版（dvh 跟的是「工具栏展开时的那一小块」，页面因此比屏幕矮一截）；
  再往下，`html` 的底色才是画在整块画布上的那层，`body` 的底色只铺到自己的盒子为止 ——
  安全区在布局视口之外，正好落在两者之间。现在按 `lvh` 铺满（工具栏展开收起也不跳），
  底色统一归 `html`，怕被挡住的内容交给 `safe-bottom` 让开（Apple 的分工）
- 没有标签的卡片，电脑端「打开 / 编辑」会挤在左下角：那一行是页脚里唯一的孩子，
  `justify-between` 把它留在了最左边。自带 `sm:ml-auto` 兜住，实测按钮右缘距卡片右边 1px
- 新建分类后「首页布局」那份清单不更新，要刷新页面才看到：「首页布局」与「首页分类」各存了
  一份 state。两块并到一个共用的出处（`PortalSettings`），新建完上面立刻多一行
- 编辑模式里一搜索就报 `useEffect 的依赖数组长度在两次渲染之间变了`：dnd-kit 内部拿传感器
  数组当 deps，而搜索时我们把传感器换成了空数组。改为始终传同一组，不可拖时走
  `useSortable` 的 `disabled`，把手跟着淡下去
- 首字母托底按整格字号显示后偏大（墨迹约占盒子 45%，比旁边的图片类图标还抢眼）：收到 0.8 倍，
  落到 36% 上下
- 编辑模式每次打开都在控制台报一条水合不一致：dnd-kit 用递增计数器生成拖动把手的
  `aria-describedby`，而 StrictMode 在开发模式下会把渲染跑两遍，服务端数到 1、2、3，
  客户端成了 2、4、6。改为给 `DndContext` 一个显式 id，两边算出来就是同一个
- 站点图标接口偶尔回 500、控制台多一条红色报错：`fetch` 拿到响应头就算成功，读正文那一步
  却在超时保护之外 —— 站点慢（响应头先到、正文拖过 4 秒）或对端中途断流时，异常直接抛给
  路由。读正文改回「取不到就是取不到」，整条取图流程也收进一层兜底：拿不到最多是首字母托底
- 图标取不到时回的那张「1×1 透明占位图」其实是坏的：IDAT 校验值不对、zlib 流根本解不开，
  浏览器只能按自己拿到的字节画，表现就是整块纯色（Dribbble、ChatGPT 那两张就是它）。
  换成一张真正合法、可解码的 1×1 透明 PNG，并补了一条测试读 IHDR 尺寸而不是数字节数
- 真实图标取到之后首字母标记没有收起来：像 Cloudflare 这种自带透明区域的图标，
  字母会从图标里透出来，看着像两层图标叠在一起。现在按加载结果分三种状态 ——
  取到真图标就收首字母，取不到（含占位图）只留首字母
- 打开「新建项目」弹窗会在控制台留一条 400 红线：网址还空着就去请求图标预览。
  空网址不再发这个请求，接口对这种请求也改回占位图而不是报错
- 站点图标去掉透明圆角：圆角在浏览器标签页、书签和桌面上会显出一圈黑底。改为满幅不透明方块，
  圆角交给系统自己加
- 拖动排序松手后会先弹回原位、再跳一次：本地顺序要等服务器回包才改，而 dnd-kit 松手时就把
  拖拽用的 transform 撤掉了。改为松手当帧先把本地顺序落定，再等服务器确认
- 编辑模式的工具条其实没有跟随滚动：`sticky` 的行程被限制在父元素高度里，而它裹在一层
  只包住自己的 `div` 里（实测那层高 46px），可粘住的行程是 0。改为直接挂在 `main` 下
- 手机端卡片的「打开」贴在左下角，与整卡的左对齐混在一起；改为靠右下
- 站点图标取不到时返回 404：控制台会多一条红色报错，图标位还留一个破图。改为回 1×1 透明占位图，
  首字母托底从占位图后面露出来，报错消失
- 首字母托底不再依赖 `onError`：图片若在 React 水合之前就失败，错误事件没人接，破图会一直留着。
  现在始终把首字母垫在图片下面，任何时刻取不到图都能看到它
- 末条卡片与页脚之间留白过大（208px）：收到 64px
- 悬停卡片菜单的「移动分类」会抛 Base UI error #31 并让整页崩溃：`DropdownMenuLabel` 必须放在
  `DropdownMenuGroup` 里
- 卡片悬浮时文字出现「清晰—模糊—清晰」的抖动：去掉 hover 位移里的缩放，缩放会让文字在动画期间被反复栅格化
- 条目弹窗里展开图标选择器后「保存 / 取消」被挤出可视区：弹窗最高一屏，表单中部滚动、底部按钮固定
- 分类描述此前只是在首页展示，设置页没有地方改：分类行新增编辑入口，可改名称与描述
- 用局域网 IP 或手机打开开发服务器时，Next 16 默认拦截非 localhost 的开发资源请求，
  `/_next/hmr` 被拦后页面停在水合之前（样式正常、点击无反应）。`next.config.ts` 默认放开
  三个私有网段，可用 `ALLOWED_DEV_ORIGINS` 追加
- 登录表单未声明 `method="post"`：脚本未执行时浏览器按 GET 原生提交，会把密码写进 URL
- `pnpm dev` 与 `pnpm build` 共用 `.next`：一次构建就可能让开发服务器引用的脚本失效，
  页面表现为样式正常但点击全无反应；开发改用独立目录 `.next-dev`

## [0.1.0] - 2026-09-19

第一个可用版本：首页、数据、认证、前台编辑、图标与备份都跑通了。

### Added

- 公开门户首页：Brand、欢迎语、即时搜索、顶部分类 Tab、按分类分区的卡片流
- 卡片包含图标、标题、描述、标签与 Open；图标取不到时回落标题首字母
- 主题系统支持 Light / Dark / System，默认跟随系统，切换不闪烁，动效遵守 `prefers-reduced-motion`
- 响应式布局：手机一列、平板两列、桌面三列；分类 Tab 在移动端横向滚动
- 搜索覆盖标题、描述、URL、域名、标签与分类，多个关键词按 AND 收窄
- SQLite + Drizzle 数据层：User / Session / Category / Item / Tag / ItemTag / IconSet / Setting / Backup 共 9 张表
- 首次访问自动执行迁移，不需要单独的迁移步骤
- 幂等种子数据（`pnpm db:seed`）：7 个分类、15 个条目、14 个标签，含一个 Private 条目
- 管理员认证：scrypt 加盐哈希、HttpOnly + SameSite=Lax + 生产环境 Secure 的会话 Cookie
- 会话默认 30 天并滑动续期：中间件推后 Cookie 到期时间，数据库侧在用到一半寿命时顺延
- 首次部署没有账号时 `/login` 切换为创建管理员，创建成功后该接口自动关闭
- 支持修改密码（并吊销其它设备会话）与退出所有设备
- 前台编辑：编辑模式下卡片可拖动排序，表单可改标题、网址、描述、分类、标签、可见性与置顶
- 卡片操作菜单：编辑、复制、移动分类、切换 Public/Private、置顶、删除（二次确认）
- 分类管理：新建、改名、上下移动、首页显示开关、删除（条目退回 Inbox 而不是被一起删掉）
- 图标系统：自动抓取站点图标（本地代理 + 按 origin 落盘缓存）、Emoji 精选表、Lucide 精选表、自定义上传
- 上传支持 PNG / JPG / WEBP / SVG，单张上限 512 KB，校验文件头并对 SVG 做清洗
- 备份与恢复：内容层面的 JSON 导出与导入、`VACUUM INTO` 数据库快照、快照列表、下载与恢复
- 设置页：外观、首页分类、数据、账号四个分区
- Prettier + ESLint + Vitest（102 项单元测试）+ GitHub Actions（格式、lint、类型、测试、构建）
- standalone 构建输出，部署产物自带依赖，不携带本地运行数据

### Security

- Private 内容由服务器端过滤：匿名响应里不含 Private 条目的标题、网址与描述，也不含未在首页显示的分类及其条目
- 所有写接口经 `withAdmin` 校验会话，前端隐藏不构成权限
- 登录失败按用户名限流并附加全局限流，不依赖客户端可伪造的转发头
- 网址只接受 http 与 https，写入、导入与恢复三条路径都做校验
- SVG 去除脚本、事件属性、远程引用、外部样式与实体声明，响应另加 CSP sandbox 与 nosniff
- 抓取站点图标时拒绝回环、link-local 与保留地址，并逐跳重新校验跳转目标
- 备份文件名按固定格式校验，上传文件名由服务端随机生成，两者都不可路径穿越
- 接口响应统一 `private, no-store`，避免共享缓存转发管理数据
